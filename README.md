# sync
A universal client-side sync layer: one configurable engine reconciles diverging replicas over an unreliable channel, with state-sync and op-sync through a single pipe and local progress that never blocks on the channel.
